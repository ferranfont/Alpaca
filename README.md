# Script para la conexión a la API de Alpaca
# La API está limitada a una determinada cantidad de valores
# y limitada a una cantidad limitada de peticiones.

pip install -r requirements.txt

python3 main.py

Incidencias wheel de la libreria TA (technical analysis):
Instala TA-Lib manualmente (Windows):

Descarga el archivo correcto desde Releases de TA-Lib en GitHub para Python 3.12 x64:

Copiar
Editar
ta_lib-0.6.3-cp312-cp312-win_amd64.whl
Luego instálalo desde la terminal:

bash
Copiar
Editar
pip install "ta_lib-0.6.3-cp312-cp312-win_amd64.whl"